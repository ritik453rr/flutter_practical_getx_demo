import 'package:flutter/material.dart';

class LabelHome extends StatelessWidget {
  const LabelHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // backgroundColor: Colors.yellow.withOpacity(0.7),
      body: Text("Label Home"),
    );
  }
}
//
